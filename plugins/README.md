# Kortyx Plugins

Kortyx plugins are flat, feature-based Studio extensions.

Each plugin folder carries its own `LICENSE.md`. Plugins are intentionally not pnpm workspaces by default; package publication belongs under `packages/*`.

## Current Plugins

- `example-plugin`: placeholder plugin used to demonstrate the plugin folder structure
